# Raw sources

Place original articles, papers, transcripts, datasets, and attachments here. Treat every item as immutable once added: the wiki may cite and interpret it, but must not alter it.

Use `assets/` for downloaded images or other attachments belonging to a source.
